<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inucalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inucalc">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".oneoneActivity" />
        <activity android:name=".onetwoActivity" />
        <activity android:name=".twooneActivity" />
        <activity android:name=".twotwoActivity" />
        <activity android:name=".threeoneActivity" />
        <activity android:name=".threetwoActivity" />
        <activity android:name=".fouroneActivity" />
        <activity android:name=".fourtwoActivity" />
        <activity android:name=".CalcuActivity" />
    </application>

</manifest>